import pc from 'picocolors';

/**
 * Project-standard CLI styling: import `pc` and use `pc.cyan(text)`, `pc.bold(pc.red(text))`, etc.
 * See `.github/instructions/09-picocolors-cli-styling.instructions.md`.
 */
export { pc };
